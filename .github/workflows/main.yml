
on:
  push:
    branches:
      - master


jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: "Login to dockerhub"
        run: docker login --username abdulrahmanabuelgheit -- password dckr_pat_b2WIzm0YBIXKuu9JCPtNE7EtqAA